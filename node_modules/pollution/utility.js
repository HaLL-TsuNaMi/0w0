const setMethodPairs = (pairs, target)=> {
	Reflect.ownKeys(pairs).forEach((key)=> {
		if(target[key] !== undefined)
			return false
		target[key] = pairs[key]
	})
} 

exports.setMethodPairs = setMethodPairs
