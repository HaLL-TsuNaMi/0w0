/*const { Command } = require('discord.js-commando');

module.exports = class CoinCommand extends Command {
	constructor(client) {
	  super(client, {
		name: 'random',
		aliases: ['rand'],
		group: 'other',
		memberName: 'random',
		guildOnly: true,
		description: 'Randomly decides out of user input'
	  });
	}

	run(msg) {
		// empty array that will be filled with user inputs
		let answers = [];

		msg.send(`Entew youw options pwease >w<: `);
		if(response.first().content) {

		}
	}
};
*/